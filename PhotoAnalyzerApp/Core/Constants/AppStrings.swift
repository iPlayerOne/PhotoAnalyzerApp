import Foundation

enum AppStrings {
    enum Button {
        static let startEditing = "Start editing"
        static let export = "Export"
    }
    
    enum Projects {
        static let title = "Projects"
        static let emptyTitle = "No projects yet"
        static let emptySubtitle = "Start editing your photos now"
        static let searchPlaceholder = "Search"
    }
}

